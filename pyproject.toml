[project]
name = "data-catalogue"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
test = [
    "pytest>=8",
    "pytest-postgresql>=5,<6",
    "apache-airflow>=2.9",
    "psycopg2-binary>=2.9",
]

[tool.pytest.ini_options]
testpaths = ["airflow/dags/tests", "db/tests"]
